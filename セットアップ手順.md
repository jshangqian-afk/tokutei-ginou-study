# 学習記録をスプレッドシートに集める — セットアップ手順

所要時間：**10分ほど**。作業は kim さん側で1回だけです。

---

## 1. スプレッドシートを作る

1. Googleドライブで新しいスプレッドシートを作成（名前の例：`特定技能1号 学習記録`）
2. メニュー **拡張機能 → Apps Script**
3. 出てきたエディタの中身を全部消して、**`Code.gs` の中身を貼りつけ**
4. 保存（フロッピーのアイコン）

## 2. 初期設定を実行する

1. Apps Script の上部にある関数選択で **`setup`** を選び、**実行**
2. 初回は権限の確認が出ます
   - 「詳細」→「（プロジェクト名）に移動」→「許可」
   - ※「このアプリは確認されていません」と出ますが、自分で書いたスクリプトなので問題ありません
3. スプレッドシートに `回答ログ` `模試結果` `学習者サマリー` `分野別サマリー` の4シートができます

## 3. ウェブアプリとして公開する

1. Apps Script の右上 **デプロイ → 新しいデプロイ**
2. 歯車アイコン → **ウェブアプリ** を選択
3. 設定：

   | 項目 | 選ぶもの |
   |---|---|
   | 次のユーザーとして実行 | **自分**（kimさんのアカウント） |
   | アクセスできるユーザー | **全員** |

   ※「全員」にしないとスタッフのスマホから送信できません。URLを知らないとアクセスできないので、URLを社外に出さなければ大丈夫です。
4. **デプロイ** を押すと `https://script.google.com/macros/s/xxxxx/exec` というURLが出ます。**このURLをコピー**

## 4. アプリにURLを埋め込む

`src/template.html` の **205行目あたり** にあるこの行を書きかえます。

```js
const GAS_URL = "";
```

↓ コピーしたURLを貼る

```js
const GAS_URL = "https://script.google.com/macros/s/xxxxx/exec";
```

保存したら、ビルドして公開します。

```bash
npm run build && npm test
git add -A && git commit -m "GAS_URL を設定" && git push
```

> **`index.html` は直接書きかえないでください。** ビルド成果物なので、手で直すと
> Service Worker の VERSION がズレて、スタッフの端末に古い版が残り続けます。

> ファイルを書きかえるのが面倒な場合は、アプリの **記録タブ → 「送信先URLを設定する」** からスマホ上で貼りつけることもできます（ただしスタッフ全員の端末で毎回必要になるので、埋め込むほうが楽です）。

## 5. スタッフに配る

すでに GitHub Pages で公開ずみです。このURLを送るだけです。

**https://jshangqian-afk.github.io/tokutei-ginou-study/**

スマホでURLを開く → ブラウザの共有メニュー → **「ホーム画面に追加」**
これでアプリのように起動し、オフラインでも使えます。

更新するときは `npm run build && git push` するだけで、
スタッフがアプリを開き直せば新しい版になります。

---

## 使いはじめ

- 初回起動時にスタッフが**なまえ**を入力します（この名前でスプレッドシートに記録されます）
- あとは普通に学習するだけ。記録は自動で送られます
- ヘッダーの名前の左にある**丸印**が送信状態です
  - 🟢 緑 … 送信ずみ
  - 🟠 オレンジ … 未送信あり（電波が戻ったら自動で送ります）
  - ⚪ 白 … URL未設定

## kimさん側で見るもの

スプレッドシートのメニュー **「学習記録」→「サマリーを更新」** を押すと、集計し直します。

| シート | 中身 |
|---|---|
| `学習者サマリー` | 人ごとの のべ回答数・正答率・学習日数・最終学習日・最新の模試点数 |
| `分野別サマリー` | 人ごと × 分野ごとの正答率（**苦手な順に並びます**。65%未満は赤） |
| `回答ログ` | 1問ずつの生ログ（日時・問題ID・正誤・選んだ番号・モード） |
| `模試結果` | 模擬試験の履歴（得点・判定・学科/実技の内訳） |

`分野別サマリー` の赤いところが、そのまま「一緒に復習すべき論点」です。

---

## 技術メモ

- **オフライン優先**：記録はまず端末に保存し、そのあと送信します。電波がなくても学習は止まりません。未送信ぶんは端末にたまり、通信が戻ったとき・アプリを開いたとき・4秒間操作が止まったときに自動送信されます
- **日時は文字列**（`YYYY-MM-DD HH:mm:ss` / Asia/Tokyo）でアプリ側が生成。GAS側で Date オブジェクトを作らないので、タイムゾーンのズレが起きません
- **列はヘッダー名で引いています**。シートの列を入れ替えても壊れません
- **サマリーは保存していません**。毎回ログから作り直すので、値がズレることがありません
- 送信は `Content-Type: text/plain` の POST（CORSのプリフライトを避けるため）。`LockService` で同時書き込みを防いでいます
- 端末IDを自動採番しているので、同じ名前で複数端末を使っても区別がつきます
